package com.example.cpu.Repositories;

import com.example.cpu.Models.Cpu;
import org.springframework.data.jpa.repository.JpaRepository;

public interface CpuRepository extends JpaRepository<Cpu, Long> {
}
